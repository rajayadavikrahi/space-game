// WebSocket backend location.
//
// Local development: the Rust server runs on your
// machine at ws://127.0.0.1:3000/ws (keep this).
//
// Production: after deploying the Rust backend
// (e.g. Render), change this to its wss:// URL and
// redeploy this client.
window.VOIDRUNNER_BACKEND_URL = "ws://127.0.0.1:3000/ws";